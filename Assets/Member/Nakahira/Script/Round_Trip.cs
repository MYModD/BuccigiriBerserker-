using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Round_Trip : MonoBehaviour
{
    [SerializeField] private Transform pintA;
    [SerializeField] private Transform pintB;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
